function outage = find_oma_min_outage(sigma,d1,d2,a,thres)

fun = @(x)4-exp(-d1^a*thres*sigma/x(1))-exp(-d2^a*thres*sigma/x(2))-exp(-d1^a*thres*sigma/x(3))-exp(-d2^a*thres*sigma/x(3));
x0 = [1,1,1];
Aeq = [1,1,1];
beq = 3;

[x,fval] = fmincon(fun,x0,[],[],Aeq,beq);